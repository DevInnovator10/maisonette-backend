<% admin_breadcrumb link_to(plural_resource_name(Spree::MarkDown), spree.admin_mark_downs_path) %>
<% admin_breadcrumb t('spree.new_mark_down') %>

<%= render :partial => 'spree/shared/error_messages', :locals => { :target => @mark_down } %>

<%= form_for [:admin, @mark_down] do |f| %>
  <fieldset class="no-border-top">
    <legend align="center"><%= t('spree.new_mark_down') %></legend>

    <%= render :partial => 'form', :locals => { :f => f } %>

    <%= render :partial => 'spree/admin/shared/new_resource_links' %>
  </fieldset>
<% end %>
